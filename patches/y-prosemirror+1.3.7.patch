diff --git a/node_modules/y-prosemirror/src/plugins/sync-plugin.js b/node_modules/y-prosemirror/src/plugins/sync-plugin.js
index dccfb76..2ade61e 100644
--- a/node_modules/y-prosemirror/src/plugins/sync-plugin.js
+++ b/node_modules/y-prosemirror/src/plugins/sync-plugin.js
@@ -257,7 +257,9 @@ const restoreRelativeSelection = (tr, relSel, binding) => {
         relSel.anchor,
         binding.mapping
       )
-      tr.setSelection(NodeSelection.create(tr.doc, anchor))
+      if (anchor !== null && anchor <= tr.doc.content.size) {
+        tr.setSelection(NodeSelection.create(tr.doc, anchor))
+      }
     } else {
       const anchor = relativePositionToAbsolutePosition(
         binding.doc,
@@ -272,8 +274,15 @@ const restoreRelativeSelection = (tr, relSel, binding) => {
         binding.mapping
       )
       if (anchor !== null && head !== null) {
-        const sel = TextSelection.between(tr.doc.resolve(anchor), tr.doc.resolve(head))
-        tr.setSelection(sel)
+        const maxPos = math.max(tr.doc.content.size - 1, 0)
+        const safeAnchor = math.min(anchor, maxPos)
+        const safeHead = math.min(head, maxPos)
+        try {
+          const sel = TextSelection.between(tr.doc.resolve(safeAnchor), tr.doc.resolve(safeHead))
+          tr.setSelection(sel)
+        } catch (e) {
+          // ignore invalid selection after sync
+        }
       }
     }
   }
